import { YouTubeFocusMode } from '../content/content';

describe('YouTubeFocusMode', () => {
  beforeEach(() => {
    document.head.innerHTML = '';
    document.body.innerHTML = '';
  });

  test('applies styles correctly', () => {
    const focusMode = new YouTubeFocusMode();
    // Add your test cases
  });
});